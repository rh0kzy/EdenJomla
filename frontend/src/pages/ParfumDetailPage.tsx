import React from 'react';
import { Typography, Box } from '@mui/material';

export default function ParfumDetailPage() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Détail du Parfum</Typography>
      <Typography>Page en construction...</Typography>
    </Box>
  );
}